// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"khepri.dev/horus/store/ent/authorizer"
	"khepri.dev/horus/store/ent/identity"
	"khepri.dev/horus/store/ent/user"
)

// AuthorizerCreate is the builder for creating a Authorizer entity.
type AuthorizerCreate struct {
	config
	mutation *AuthorizerMutation
	hooks    []Hook
}

// SetOwnerID sets the "owner_id" field.
func (ac *AuthorizerCreate) SetOwnerID(u uuid.UUID) *AuthorizerCreate {
	ac.mutation.SetOwnerID(u)
	return ac
}

// SetPrimaryID sets the "primary_id" field.
func (ac *AuthorizerCreate) SetPrimaryID(s string) *AuthorizerCreate {
	ac.mutation.SetPrimaryID(s)
	return ac
}

// SetPrimary sets the "primary" edge to the Identity entity.
func (ac *AuthorizerCreate) SetPrimary(i *Identity) *AuthorizerCreate {
	return ac.SetPrimaryID(i.ID)
}

// SetOwner sets the "owner" edge to the User entity.
func (ac *AuthorizerCreate) SetOwner(u *User) *AuthorizerCreate {
	return ac.SetOwnerID(u.ID)
}

// Mutation returns the AuthorizerMutation object of the builder.
func (ac *AuthorizerCreate) Mutation() *AuthorizerMutation {
	return ac.mutation
}

// Save creates the Authorizer in the database.
func (ac *AuthorizerCreate) Save(ctx context.Context) (*Authorizer, error) {
	return withHooks(ctx, ac.sqlSave, ac.mutation, ac.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (ac *AuthorizerCreate) SaveX(ctx context.Context) *Authorizer {
	v, err := ac.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (ac *AuthorizerCreate) Exec(ctx context.Context) error {
	_, err := ac.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (ac *AuthorizerCreate) ExecX(ctx context.Context) {
	if err := ac.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (ac *AuthorizerCreate) check() error {
	if _, ok := ac.mutation.OwnerID(); !ok {
		return &ValidationError{Name: "owner_id", err: errors.New(`ent: missing required field "Authorizer.owner_id"`)}
	}
	if _, ok := ac.mutation.PrimaryID(); !ok {
		return &ValidationError{Name: "primary_id", err: errors.New(`ent: missing required field "Authorizer.primary_id"`)}
	}
	if v, ok := ac.mutation.PrimaryID(); ok {
		if err := authorizer.PrimaryIDValidator(v); err != nil {
			return &ValidationError{Name: "primary_id", err: fmt.Errorf(`ent: validator failed for field "Authorizer.primary_id": %w`, err)}
		}
	}
	if _, ok := ac.mutation.PrimaryID(); !ok {
		return &ValidationError{Name: "primary", err: errors.New(`ent: missing required edge "Authorizer.primary"`)}
	}
	if _, ok := ac.mutation.OwnerID(); !ok {
		return &ValidationError{Name: "owner", err: errors.New(`ent: missing required edge "Authorizer.owner"`)}
	}
	return nil
}

func (ac *AuthorizerCreate) sqlSave(ctx context.Context) (*Authorizer, error) {
	if err := ac.check(); err != nil {
		return nil, err
	}
	_node, _spec := ac.createSpec()
	if err := sqlgraph.CreateNode(ctx, ac.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	ac.mutation.id = &_node.ID
	ac.mutation.done = true
	return _node, nil
}

func (ac *AuthorizerCreate) createSpec() (*Authorizer, *sqlgraph.CreateSpec) {
	var (
		_node = &Authorizer{config: ac.config}
		_spec = sqlgraph.NewCreateSpec(authorizer.Table, sqlgraph.NewFieldSpec(authorizer.FieldID, field.TypeInt))
	)
	if nodes := ac.mutation.PrimaryIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: false,
			Table:   authorizer.PrimaryTable,
			Columns: []string{authorizer.PrimaryColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(identity.FieldID, field.TypeString),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.PrimaryID = nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := ac.mutation.OwnerIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2O,
			Inverse: true,
			Table:   authorizer.OwnerTable,
			Columns: []string{authorizer.OwnerColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(user.FieldID, field.TypeUUID),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.OwnerID = nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// AuthorizerCreateBulk is the builder for creating many Authorizer entities in bulk.
type AuthorizerCreateBulk struct {
	config
	builders []*AuthorizerCreate
}

// Save creates the Authorizer entities in the database.
func (acb *AuthorizerCreateBulk) Save(ctx context.Context) ([]*Authorizer, error) {
	specs := make([]*sqlgraph.CreateSpec, len(acb.builders))
	nodes := make([]*Authorizer, len(acb.builders))
	mutators := make([]Mutator, len(acb.builders))
	for i := range acb.builders {
		func(i int, root context.Context) {
			builder := acb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*AuthorizerMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, acb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, acb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, acb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (acb *AuthorizerCreateBulk) SaveX(ctx context.Context) []*Authorizer {
	v, err := acb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (acb *AuthorizerCreateBulk) Exec(ctx context.Context) error {
	_, err := acb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (acb *AuthorizerCreateBulk) ExecX(ctx context.Context) {
	if err := acb.Exec(ctx); err != nil {
		panic(err)
	}
}
