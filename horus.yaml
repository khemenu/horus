grpc:
  host: 0.0.0.0
  port: 35122
  gateway:
    enabled: false
    host: 0.0.0.0
    port: 35124

http:
  host: 0.0.0.0
  port: 35123

db:
  driver: sqlite3
  source: file:horus.db?cache=shared&_fk=1

hr:
  connect:
    with: db
    db:
      driver: sqlite3
      source: file:horus.db?cache=shared&_fk=1

log:
  enabled: true
  format: text
  level: warn

debug:
  enabled: true
  unsecured: true
  mem_db:
    enabled: false
    users:
      - alias: ra
        password: 1234
