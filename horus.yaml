grpc:
  host: 0.0.0.0
  port: 35122

http:
  host: 0.0.0.0
  port: 35123

db:
  driver:
  source:

client:
  connect_with: db
  db:
    driver: sqlite3
    source: file:horus.db?cache=shared&_fk=1
    use_bare: true

debug:
  enabled: true
  unsecured: true
  mem_db:
    enabled: true
    users:
      - alias: ra
        password: 1234
