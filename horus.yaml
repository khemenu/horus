grpc:
  host: 0.0.0.0
  port: 35122

http:
  host: 0.0.0.0
  port: 35123

kube:
  enabled: true

db:
  driver:
  source:

client:
  db:
    enabled: true
    use_bare: true
    driver: sqlite3
    source: file:horus.db?cache=shared&_fk=1

debug:
  enabled: true
  unsecured: true
  mem_db:
    enabled: true
    users:
      - name: ra
        password: 1234
